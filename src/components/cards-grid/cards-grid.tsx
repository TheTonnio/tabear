import React from 'react';

const CardsGrid = (props: any) => {
  const {
    children
  } = props;
  return (
    <div>
      {
        children
      }
    </div>
  );
};

export default CardsGrid;
